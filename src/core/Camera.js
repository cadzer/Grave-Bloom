import { GAME } from '../config/GameConfig.js';

// Re-export for backward compatibility. Prefer importing from GameConfig.
export const GAME_WIDTH = GAME.WIDTH;
export const GAME_HEIGHT = GAME.HEIGHT;
